/*
 * menu_layout.h — SC2 .SC2Layout types and API (thin wrapper).
 *
 * All definitions live in stb_sc2layout.h. This header exists for backward
 * compatibility so existing files that include "menu_layout.h" keep working.
 */
#ifndef sc2_layout_h
#define sc2_layout_h

#include "../common/stb_sc2layout.h"

#endif
