/* Generated by fdfbindgen. Do not edit by hand. */
#ifndef LEADERBOARD_H
#define LEADERBOARD_H

#include "../g_local.h"

typedef struct LeaderBoard_s {
    LPFRAMEDEF Leaderboard;
    LPFRAMEDEF LeaderboardBackdrop;
    LPFRAMEDEF LeaderboardTitle;
    LPFRAMEDEF LeaderboardListContainer;
} LeaderBoard_t;


static inline BOOL LeaderBoard_Bind(LeaderBoard_t *out, LPFRAMEDEF bind_root) {
    BOOL ok = true;
    if (!out) {
        return false;
    }
    memset(out, 0, sizeof(*out));
    out->Leaderboard = bind_root;
    if (!out->Leaderboard) {
        BZ_FDF_REPORT_MISSING("Leaderboard");
        ok = false;
    }
    BZ_FDF_BIND_CHILD(out, LeaderboardBackdrop, bind_root, "LeaderboardBackdrop");
    BZ_FDF_BIND_CHILD(out, LeaderboardTitle, bind_root, "LeaderboardTitle");
    BZ_FDF_BIND_CHILD(out, LeaderboardListContainer, bind_root, "LeaderboardListContainer");
    return ok;
}

static inline BOOL LeaderBoard_Load(LeaderBoard_t *out) {
    return out &&
           UI_EnsureFDF("UI\\FrameDef\\UI\\EscMenuTemplates.fdf") &&
           UI_EnsureFDF("UI\\FrameDef\\UI\\LeaderBoard.fdf") &&
           LeaderBoard_Bind(out, UI_FindFrame("Leaderboard"));
}

#endif /* LEADERBOARD_H */
